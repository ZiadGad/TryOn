version: '3'
services:
  tryon-app:
    build:
      context: .
      # args:
      #   - NODE_ENV=production
      target: production
    environment:
      - NODE_ENV=production
    command: npm run start:prod
